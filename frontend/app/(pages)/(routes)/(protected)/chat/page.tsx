import React from "react";
import "@/app/(pages)/(routes)/(protected)/game/style.css";

const page = () => {
  return <div className="w-full h-full"
  style={
    {
      display: "flex",
      justifyContent: "center",
      alignItems: "center",
    }
  }
  > No Conversation selected , Let&apos;s Talk!</div>;
};

export default page;
